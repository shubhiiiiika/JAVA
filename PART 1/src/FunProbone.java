

public class FunProbone {
    public static void main(String[] args) {
        // Guess the output
        int[]arr=new int[5];
        System.out.println(arr[0]);
        /*
        0 -> the output why ? the array isn't initialised.
        Array stores default value '0' in all the indices
         */

    }
}
